import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Simple Todo Application',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
